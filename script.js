const name = prompt("Ismingizni kiriting");
const id = prompt("space idingizni kiriting");
const password = prompt("Space passwordingizni kiriting");

const user = {
  name: name,
  id: id,
  password: password
};

console.log(user);
